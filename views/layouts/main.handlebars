<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Voyager</title>
    <link rel="icon" type="image/x-icon" href="/assets/favicon.png">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
    <script src="https://polyfill.io/v3/polyfill.min.js?features=default"></script>
</head>
<body style="background-size: cover;">
<header class="container-fluid text-center p-1 rounded-3">
    <div class="row align-items-center" id="header">
        <div class="col">
            <nav class="nav justify-content-start bg-custom-purple border-purple border rounded-3">
                <a class="nav-link text-light" href="/">Home</a>
                {{#if loggedIn}}
                <a class="nav-link text-light" href="/feed">Feed</a>
                <a class="nav-link text-light" href="/post">Post</a>
                <a class="nav-link text-light" href="/dashboard">Dashboard</a>
                {{else}}
                <a class="nav-link text-light" href="" data-bs-toggle="modal" data-bs-target="#loginModal">Feed</a>
                <a class="nav-link text-light" href="" data-bs-toggle="modal" data-bs-target="#loginModal">Post</a>
                {{/if}}
            </nav>
        </div>
        <div class="col-6">
            <h1 class="text-center text-primary h1">Voyager</h1>
        </div>
        <div class="col justify-content-end">
            <div class="container d-flex align-items-center">
                <div class="input-group">
                    <input name="location-query" type="text" class="form-control" id="location-query" placeholder="🔎 Search for a location">
                </div>
                {{#if loggedIn}}
                <button type="button" class="btn btn-primary" id="logout">
                    logout
                </button>
                {{else}}
                <button type="button" class="btn btn-primary" data-bs-toggle="modal" data-bs-target="#loginModal">
                    Login
                </button>
                {{/if}}
            </div>
            <div class="container">
                {{> loginModal }}
            </div>
        </div>
    </div>
</header>
<main class="container mt-5">
    {{{ body }}}
</main>
{{#if loggedIn}}
<script src="/js/logout.js"></script>
{{/if}}
<script src="/js/locationQuery.js"></script>
<script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
